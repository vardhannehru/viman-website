# Backend

Nothing lives here yet. The VIMAN website in `../frontend` needs no server, database or API today — even the checklist progress is saved in each visitor's own browser.

Use this folder when the site needs server-side code, for example a contact form that sends student enquiries to VIMAN, or bookings for guidance sessions.
